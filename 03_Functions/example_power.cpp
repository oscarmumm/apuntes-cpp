#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    // in this case we use the pow function from <cmath> library
    double result = pow(5.5, 2);
    cout << result;
    return 0;
}